name: ethereal_genui_example
description: A minimal example app for the ethereal_genui package.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.12.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ethereal_genui:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
