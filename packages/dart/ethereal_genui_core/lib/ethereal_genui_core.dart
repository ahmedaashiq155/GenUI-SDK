/// Ethereal GenUI Core — pure Dart, zero Flutter dependencies.
///
/// Contains the block schema catalog, JSON patch (RFC 6902), message segment
/// parser, and option normalizer. Safe to use in any Dart environment
/// (server, CLI, web worker) without a Flutter dependency.
library;

export 'src/genui_schema.dart';
export 'src/json_patch.dart';
export 'src/message_segments.dart';
export 'src/genui_options.dart';
export 'src/agui_transport.dart';
